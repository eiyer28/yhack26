[build]
builder = "nixpacks"
buildCommand = "pip install uv && uv sync --frozen"

[deploy]
startCommand = "uv run uvicorn server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/spot"
restartPolicyType = "on_failure"
